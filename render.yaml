services:
  - type: web
    name: examhall
    runtime: node
    buildCommand: npm run render-build
    startCommand: cd server && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
